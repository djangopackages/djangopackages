[bumpver]
current_version = "2022.2.11"
version_pattern = "YYYY.MM.INC1"
commit_message = ":bookmark: bump version {old_version} -> {new_version}"
commit = true
push = false  # diabled for GitHub Actions
tag = true

[bumpver.file_patterns]
"core/__init__.py" = [
    '__version__ = "{version}"',
]
# "bumpver.toml" = [
#     'current_version = "{version}"',
# ]

